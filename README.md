# git-remote-test

## Random Color Page

The repository includes a simple HTML page (`random-color-page.html`) that changes the background color to a random RGB value each time the button is clicked. The page demonstrates basic DOM manipulation and CSS transitions.
